export interface UserPassingChat {
  id: string | number;
  name: string;
  username: string;
  imageUrl: string;
}

export interface UserInterface {
  id: string;
  name: string;
  username: string;
  email: string;
}
